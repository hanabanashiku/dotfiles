// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+shift+k",
        "command": "vim.remap",
        "when": "editorTextFocus && vim.mode == 'Normal'",
        "args": {
            "commands": [
                "workbench.action.peekDefinition"
            ]
        }
    }
]